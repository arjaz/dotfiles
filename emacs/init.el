;;; init.el ---  My configuration

;;; Commentary:
;;; just load the org file with the actual config

;;; Code:
(org-babel-load-file (expand-file-name "~/.dotfiles/emacs/litemacs.org"))

(provide 'init)
;;; init ends here
